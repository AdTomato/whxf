<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authine.cloudpivot.web.api.mapper.TrainInfoListMapper">

    <select id="getWorkInfo" parameterType="java.lang.String"
            resultType="com.authine.cloudpivot.web.api.entity.WorkInfo">
        SELECT *
        FROM i1e6k_work_info
        WHERE id = #{id}
    </select>

<!--
<select id="getTrainInfoList" parameterType="java.lang.String"
            resultType="com.authine.cloudpivot.web.api.dto.TrainInfoList">
        SELECT a.*,b.*
        FROM i1e6k_train_info a,i1e6k_checkdetail b
        WHERE a.id = #{id}
           AND b.parentId = #{id}
    </select>
-->

    <resultMap id="trainInfoList" type="com.authine.cloudpivot.web.api.dto.TrainInfoList">
        <id column="id" property="id"/>
        <collection property="checkDetails" column="id"
                    select="com.authine.cloudpivot.web.api.mapper.TrainInfoListMapper.getCheckDetail"/>

    </resultMap>
    <select id="getTrainInfoList" resultMap="trainInfoList">
        SELECT *
        FROM i1e6k_train_info
        WHERE id = #{id}
    </select>

    <select id="getCheckDetail" parameterType="java.lang.String"
            resultType="com.authine.cloudpivot.web.api.entity.CheckDetail">
         SELECT *
        FROM i1e6k_checkdetail b
        WHERE  parentId = #{id}
    </select>
<!--
    <select id="getScaleTestResultByParentId" parameterType="java.lang.String"
            resultType="com.authine.cloudpivot.web.api.entity.ScaleTestResult">
       SELECT *
        FROM igpgf_scale_test_result
        WHERE parentId = #{id}
    </select>

    <select id="getScaleTestAcoresByParentId" parameterType="java.lang.String"
            resultType="com.authine.cloudpivot.web.api.entity.ScaleTestAcore">
       SELECT *
        FROM igpgf_scale_test_acore
        WHERE scaleTest = #{id}
    </select>-->



</mapper>